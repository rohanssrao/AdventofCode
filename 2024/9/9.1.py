(fs := [i // 2 if i % 2 == 0 else '.' for i, n in enumerate(list(open(0).readline().strip())) for _ in range(int(n))], fs := fs[:-fs.count('.')] + [n for n in fs[-fs.count('.'):] if n != '.'], dots := [i for i,n in enumerate(fs) if n == '.'], fs := (lambda: [ (fs.__setitem__(d, fs[-(i + 1)]), fs.__setitem__(-(i + 1), '.')) for i, d in enumerate(dots) ] and fs)(), print(sum(i*int(n) for i,n in enumerate(fs) if n != '.')))
