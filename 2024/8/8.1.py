print(len(set((y,x) for grid in ([list(l.strip()) for l in open(0)],) for antennas in ({ c: set((y, x) for y in range(len(grid)) for x in range(len(grid[y])) if grid[y][x] == c) for c in set(grid[y][x] for y in range(len(grid)) for x in range(len(grid[y]))) - set('.') },) for locs in antennas.values() for a1, a2 in __import__('itertools').permutations(locs, 2) for y,x in ((a2[0] + (a2[0] - a1[0]), a2[1] + (a2[1] - a1[1])),) if 0 <= y < len(grid) and 0 <= x < len(grid[0]) )))
